# Address and ports published on the Docker host.
FARO_BIND_ADDRESS=0.0.0.0
FARO_UI_PORT=1787
FARO_DNS_PORT=53

# Optional image source and release. The defaults track the latest release.
FARO_IMAGE_NAMESPACE=tabierto
FARO_VERSION=latest
